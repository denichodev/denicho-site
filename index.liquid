---
layout: layout.html
---
Some content.

{% for post in collections.post reversed %}
  <h2>
    <a href="{{ post.url }}">{{ post.data.title }} - {{ post.page.date | date: '%B %d, %Y' }}</a>
  </h2>
  <p>{{ post.content }}</p>
{% endfor %}
